import { InvokeFunctionPipe } from './invoke-function.pipe';

describe('InvokeFunctionPipe', () => {
  it('create an instance', () => {
    const pipe = new InvokeFunctionPipe();
    expect(pipe).toBeTruthy();
  });
});
